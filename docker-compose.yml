services:
  db:
    image: postgres:16-alpine
    container_name: school-assistant-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: school_assistant
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d school_assistant"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
